name: Build Hyperion Client
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Download Gradle 8.10
        run: |
          curl -sSL -o /tmp/gradle.zip \
            https://services.gradle.org/distributions/gradle-8.10-bin.zip
          unzip -q /tmp/gradle.zip -d "$HOME/gradle-dist"

      - name: Show which Gradle is which
        run: |
          echo "--- system gradle ---"
          which -a gradle || true
          gradle --version || true
          echo "--- downloaded gradle ---"
          "$HOME/gradle-dist/gradle-8.10/bin/gradle" --version

      - name: Build
        run: |
          "$HOME/gradle-dist/gradle-8.10/bin/gradle" build --stacktrace

      - uses: actions/upload-artifact@v4
        with:
          name: Hyperion-JAR
          path: build/libs/*.jar
